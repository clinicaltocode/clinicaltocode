---
phase: 06-moderation
plan: "07"
type: execute
wave: 5
depends_on:
  - "06-05"
  - "06-06"
files_modified: []
autonomous: false
requirements:
  - MOD-01
  - MOD-02
  - MOD-03
  - MOD-04

must_haves:
  truths:
    - "All four MOD requirements are satisfied and verifiable by a human tester"
    - "Full test suite passes before human verify step runs"
  artifacts: []
  key_links: []
---

<objective>
Final test suite run and human verification checkpoint for Phase 6 Moderation.

Purpose: Confirm all four MOD requirements are satisfied end-to-end before marking the phase complete.
Output: Human approval + phase 6 complete status.
</objective>

<execution_context>
@/Users/potts/Claude Projects/clinicaltocode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/potts/Claude Projects/clinicaltocode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-moderation/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files>tests/forum/moderation.test.ts</files>
  <read_first>
    - .planning/phases/06-moderation/06-VALIDATION.md (sampling rate and requirements map)
  </read_first>
  <action>
Run the full Vitest suite. All tests must pass before the human-verify checkpoint.

```bash
npx vitest run
```

If any test fails:
1. Read the failure output carefully
2. Fix the failing code (do NOT modify test assertions — the tests are the contract)
3. Re-run until the full suite is green

Also run TypeScript check:
```bash
npx tsc --noEmit
```

Both must exit 0 before proceeding to the human-verify checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/potts/Claude Projects/clinicaltocode" && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - Output contains "0 failed" or equivalent green result
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Full test suite green, TypeScript clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — Phase 6 Moderation end-to-end</name>
  <files></files>
  <action>
Pause for human verification of the complete Phase 6 Moderation implementation. The executor presents the verification checklist and waits for the human to test and approve.

Prerequisites for the human tester:
1. Apply migration in Supabase SQL Editor: `supabase/migrations/20260322000001_add_moderation.sql`
2. Set is_admin on your account: `UPDATE user_profiles SET is_admin = true WHERE id = '<your-user-id>';`
3. Ensure dev server is running: `npm run dev`
  </action>
  <what-built>
Phase 6 Moderation — complete implementation:
- Wave 0 test stubs (moderation.test.ts) — MOD-01, MOD-02, MOD-03 logic tested
- DB migration (20260322000001_add_moderation.sql) — content_reports table + RLS
- Middleware admin guard — /admin/* routes protected
- Moderation data layer (lib/moderation/types.ts + actions.ts) — 7 Server Actions
- ReportButton + ReportModal components — Flag icon, Base UI Dialog, 5 pre-set reasons
- GuidelinesBanner component — dismissible, localStorage persistence
- /community-guidelines page — 5 sections (De-identification, Professional Conduct, Acceptable Content, Enforcement, Contact)
- PostItem + ThreadCard extended — soft-delete placeholder, ReportButton
- /admin dashboard with 3 stat cards
- /admin/reports — report queue with Mark Reviewed, Delete Content, Ban User actions
- /admin/users — user list with Ban/Unban actions
- /admin/content — removed threads/posts with Restore and Delete Permanently actions
  </what-built>
  <how-to-verify>
**MOD-04: Community Guidelines**
1. Navigate to http://localhost:3000/community-guidelines
2. Verify page loads with title "Community Guidelines"
3. Confirm all five sections are present: "De-identification & Patient Privacy", "Professional Conduct", "Acceptable Content", "Enforcement", "Contact & Reporting"
4. Navigate to http://localhost:3000/forum — confirm the dismissible banner appears above the category list with link to /community-guidelines
5. Click the × button — confirm banner disappears
6. Refresh the page — confirm banner does NOT reappear (localStorage persisted)

**MOD-01: Report flow**
7. Log in as a non-admin user
8. Navigate to any forum thread
9. Confirm "Report" button (Flag icon) appears on posts authored by other users
10. Confirm "Report" button is NOT visible on your own posts
11. Click Report on a post — confirm modal opens with "Report this content" title and reason dropdown
12. Select a reason (e.g., "Spam"), submit — confirm confirmation message "Report submitted. We'll review it."
13. Confirm the button changes to "Reported" (grayed out)
14. Try to open the modal again by clicking the now-grayed button — confirm nothing happens

**MOD-02: Admin report queue**
15. Navigate to http://localhost:3000/admin (must be logged in as admin)
16. Confirm dashboard loads with "Pending Reports" stat card (should show at least 1 from step 12)
17. Click "Pending Reports" card or navigate to http://localhost:3000/admin/reports
18. Confirm the report from step 12 appears in the table with Reason badge and "Pending" status
19. Click "Mark Reviewed" — confirm status changes to "Reviewed"

**MOD-03: Soft-delete and ban**
20. On the same report row, click "Delete Content" — confirm the targeted post/thread is soft-deleted (disappears from forum, shows placeholder in thread detail)
21. Navigate to http://localhost:3000/admin/users
22. Find a test user — click "Ban User" — confirm status badge changes to "Banned"
23. Click "Unban" — confirm status returns to "Active"
24. Navigate to http://localhost:3000/admin/content — confirm the soft-deleted content from step 20 appears in the Removed Posts or Removed Threads tab
25. Click "Restore" on the removed item — confirm it reappears in the forum
  </how-to-verify>
  <verify>
    <automated>MISSING — this task is a human verification checkpoint; no automated check</automated>
  </verify>
  <done>Human tester has verified all four MOD requirements end-to-end and typed "approved".</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Human approval required. All automated checks (vitest run, tsc --noEmit) must pass before the checkpoint.
</verification>

<success_criteria>
- Full test suite green
- All four MOD requirements verified end-to-end by human tester
- Phase 6 Moderation approved for completion
</success_criteria>

<output>
After completion, create `.planning/phases/06-moderation/06-07-SUMMARY.md`
</output>
